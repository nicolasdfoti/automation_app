"""Pure business logic shared by the Streamlit pages and the future FastAPI layer.

Each module is a verbatim relocation of computation currently living inside
Streamlit pages. UI code (st.*, ui.*) stays in the pages.
"""